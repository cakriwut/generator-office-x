language: ruby
rvm:
- 2.4.1
sudo: required

cache: bundler

before_install:
  - wget https://github.com/jgm/pandoc/releases/download/2.6/pandoc-2.6-1-amd64.deb
  - sudo dpkg -i pandoc-2.6-1-amd64.deb
  - rm pandoc-2.6-1-amd64.deb

install:
  - bundle install

script: bundle exec jekll build
